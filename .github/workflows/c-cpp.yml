name: C/C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: cmake_gen
      run: cmake .
    - name: cmake_build
      run: cmake --build .
    - name: switching_to_cmd
      run: cmd
    - name: moving_dependencies
      run: move /Y dep\runtime\* Debug
    - name: exiting_from_cmd
      run: exit
    - name: run_app
      run: "Debug/armasimu"
